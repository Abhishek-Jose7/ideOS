{
  "mcpServers": {
    "ideos": {
      "command": "npx",
      "args": [
        "-y",
        "ideos",
        "mcp"
      ],
      "env": {
        "IDEOS_WORKSPACE": "${workspaceFolder}/.ideos"
      }
    }
  }
}
